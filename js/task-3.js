function filterArray(numbers, value) {
    var result = []; // boş bir dizi oluştur

    for (var i = 0; i < numbers.length; i++) { /
            result.push(numbers[i]); 
        }
    }

    return result; 
}
